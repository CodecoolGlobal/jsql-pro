createTextInput, errorMessage
CREATE TABLE myTable (string name, string nickname, int32 age, int32 weight);, It should be good.
CREATE TABLE movies (string title, int32 release date, int32 runtime, string genre);, It should be good.
CREATE TABLE without; (string name, string nickname, int32 age, int32 weight), Please append ;.
CREATE TABLE invalidFormat (name, string nickname, int32 age, int32 weight);, There isn't string before name.
CREATE TABLE invalidFormat2 (string name, string nickname, int32 age, int32);, There isn't weight after int32.
CREATE TABLE invalidFormat3 (string name, string int32 age, int32 weight);, There are string and int32 as well before age.
CREATE TABLE invalidFormat4 (b name, s nickname, i age, i weight);, Invalid format before columns.
SELECT TABLE(string name, string nickname, int32 age, int32 weight);, Write CREATE instead of SELECT.